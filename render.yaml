services:
  - type: web
    name: xauusd-dashboard-izrr
    runtime: node
    plan: starter
    numInstances: 1
    buildCommand: npm run build
    startCommand: npm start
    healthCheckPath: /api/health
    autoDeployTrigger: commit
